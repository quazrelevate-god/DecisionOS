/**
 * Fixture: stands in for components/ds/TerminalBlock.jsx in the lint allowlist.
 * Monospace is legal here and nowhere else. Asserted by tokens/lint tests.
 */
export const AllowedMono = () => <pre className="font-mono text-code text-terminal-fg">cash_position ₹18,42,300</pre>;
