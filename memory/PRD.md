## Changelog
- 2026-06: **Integration — document OCR now uses the user's own Google Gemini key.** Added `GEMINI_API_KEY` to backend/.env (valid Generative Language API key despite unusual `AQ.` prefix — verified via models.list 200 OK). Installed `google-genai==2.10.0`. Rewrote `ai_extract_document()` (server.py) to prefer a module-level `google-genai` client (user key) — `_gemini_doc_sync` runs `client.models.generate_content(model="gemini-2.5-flash", contents=[Part.from_bytes(file), prompt], config=response_mime_type="application/json")` wrapped in `asyncio.to_thread` — and fall back to the existing Emergent-key `LlmChat` VISION_MODEL path if the key is missing/errors. Verified end-to-end: backend logs "Gemini document-OCR enabled with user key"; extracted a test invoice image → doc_type=purchase_bill, confidence 1.0, correct summary. **MILESTONE: all 3 AI providers now run on the user's own keys** — OpenAI (gpt-4o-transcribe, voice), Anthropic (Claude Sonnet 4.6, reasoning), Gemini (gemini-2.5-flash, doc OCR). Emergent key retained only as fallback everywhere. DEPLOY NOTE: add `GEMINI_API_KEY` in Deployment Settings → Environment Variables and redeploy. Added `ANTHROPIC_API_KEY` to backend/.env and `CLAUDE_KEY = ANTHROPIC_API_KEY or EMERGENT_LLM_KEY` in core.py (exported). Swapped `api_key=EMERGENT_LLM_KEY` → `api_key=CLAUDE_KEY` at all 13 Claude `LlmChat(...).with_model(*LLM_MODEL)` call sites (ai_extract, ai_score_tasks, ai_score_contact, ai_meeting_notes, ai_execution_plan, ai_step_assist, ai_clarify_directive, ai_work_coach, ask_ai, ai_map_spreadsheet, ai_capture_triage in server.py + onboarding_suggest, onboarding_os_blueprint in routers/onboarding.py). Left on Emergent key: Gemini doc-vision (`ai_extract_document`, VISION_MODEL, server.py:3361) and Whisper STT fallback (server.py:551). Falls back to Emergent key automatically if ANTHROPIC_API_KEY is unset. Verified end-to-end: CLAUDE_KEY resolves to `sk-ant-api03…`, and a live `ai_extract` call succeeded returning a correctly-structured task. DEPLOY NOTE: add `ANTHROPIC_API_KEY` in Deployment Settings → Environment Variables and redeploy for production. Added `OPENAI_API_KEY` and `OPENAI_STT_MODEL` (default `gpt-4o-transcribe`) to backend/.env. Rewrote `transcribe_audio()` (server.py) to prefer a module-level `AsyncOpenAI` client (user key) with the newer model, and fall back to the existing Emergent-key Whisper (`whisper-1`) path if the key is missing/invalid or a call errors — so Decision Desk & Meeting Notes voice capture never breaks. Language/prompt handling (Auto/EN/TA/Tanglish) preserved via `_stt_lang_prompt`. Claude (structuring) and Gemini (doc OCR) still run on the Emergent key. Verified end-to-end: backend log confirms "OpenAI transcription enabled with model 'gpt-4o-transcribe'"; synthesized a test phrase via OpenAI TTS (user key) and transcribed it back through the pipeline — exact match. DEPLOY NOTE: add `OPENAI_API_KEY` (and optionally `OPENAI_STT_MODEL`) in Deployment Settings → Environment Variables and redeploy for it to take effect on production. Built a complete, brand-styled, printable manual at `/app/frontend/public/manual/index.html` (served at `/manual/index.html`). Covers web + mobile, one manual for everyone with clearly-marked "For Owners" / "For Team Members" badges, 19 step-by-step sections (login, roles, Decision Desk capture, approvals, My Work, AI Execution Guide, proof of work, leave, CEO Brief, People, Company Brain, Capture+WhatsApp, Review Queue, Meetings, Settings, Mobile, FAQ). 14 real screenshots (11 web @1440×900 + 3 mobile @390×844) captured via headless Playwright driven from the shell (the screenshot_tool runs in an isolated FS and cannot write to /app, so browsers were installed with `playwright install chromium` and a capture script saved PNGs directly into `public/manual/`). Styled to match the app (Chivo/IBM Plex Mono, brand red/blue/yellow, brutalist offset shadows, rounded-none), with a sticky TOC, numbered step cards, tip callouts, role badges, a floating "Print / Save as PDF" button, and `@media print` rules. Verified: `/manual/index.html` returns 200 with correct title, all 14 images 200 image/png, renders correctly in browser. Deploy note: it's in `public/` so it ships with the production build — accessible at `https://decisionos.biz/manual/index.html` after redeploy. Applied: (1) `People.js` inline `types` arrays → module constants `CUSTOMER_TYPES`/`VENDOR_TYPES`; (2) `AuthContext` provider value wrapped in `useMemo([user,tenant,loading])` (functions close only over stable refs). Declined as FALSE POSITIVES (would break the live app): all "missing hook dependency" additions (scanner wants `api`/`setState`/`localStorage`/`URLSearchParams`/`data` in deps → infinite loops or undefined vars; every flagged effect is an intentional run-once with documented eslint-disable); `useTheme.js` "move auth tokens out of localStorage" is factually wrong (it stores only theme, not auth — auth is HttpOnly cookies); "5 console.log" — there are zero console.log, the 4 console.debug are intentional non-blocking catch-block logs. Deferred (high regression risk, no functional gain on live prod): Python complexity refactors (register/process_voice_note/operating_score/etc.), large-component splits (Inbox/Login/CompanyDialog/Layout), type-hint coverage, and low-value useMemo micro-opts (Leave/Tasks/Team). Verified: frontend compiles, login/session + People tabs work. This is the 4th recurrence of this automated report; stance unchanged — do refactors incrementally per-module with tests, not as sweeping churn. User report: an employee's WhatsApp task ("prepare an estimate") landed in the OWNER's queue and needed owner approval before reaching the person, instead of going to the relevant department. Root cause: `persist_capture_draft` mapped classification → a HARDCODED role key (`finance`/`sales`/`purchase`/`hr`) and asked the AI to pick a department from a fixed list; when those keys didn't match the tenant's ACTUAL role keys (e.g. `finance_and_insurance_manager`, `accounts_and_admin`, `sales_consultant`, `sales_estimator`), `reviewer` fell back to `owner` — which was true for almost every non-demo tenant. Fix (server.py): replaced `CAPTURE_ROUTING` with `INTENT_BY_CLASS` + `DEPT_HINTS` and a new `_resolve_reviewer_role(tenant_id, troles, intent)` that fuzzy-matches the department intent against the tenant's real role keys/labels. Routing now sends routine work (tasks, sales, purchase, hr, meetings, workflows) to the resolved DEPARTMENT queue; owner is used ONLY for formal approvals/decisions, policy/high-risk, or high-value (≥ threshold, with sign-off). Owner still sees ALL captures (unchanged). Department reviewer approves & assigns (unchanged). Also expanded the AI triage `department` enum to [sales, finance, purchase, hr, operations, production, marketing, owner] with guidance to use owner only for policy/approvals/big commitments. Verified live across 3 real tenants: estimate→sales_consultant / sales_estimator / boutique_sales; procurement→procurement; payment follow-up→finance_and_insurance_manager; all needs_owner=False. Backend-only, no data migration. (1) **Removed the Messages panel from My Work** — the task list is now full-width (`mywork-list`); messages remain accessible via the notification bell in the top bar. Dropped the now-unused notifications query. (2) **Ingest "View" button** — new self-contained `FilePreview` component in `Ingest.js` (opens images in a lightbox dialog, PDFs/sheets in a new tab via `/api/files/{fname}`), added to the Review panel header (`ingest-review-view-file`) and every recent-upload row (`ingestion-view-<id>`). (3) **Proof-of-work gallery upgrade in task cards (`MyWork.js`)** — attachments now render as a labelled "Proof of work · N" block visible to the assignee AND the owner in All Tasks; photos are clickable thumbnails opening a full-size lightbox (`photo-lightbox-<id>`), voice notes play inline via native audio controls. Verified live (owner, All Tasks → Completed): Messages gone/list full-width, 19 View buttons on ingest, proof block renders voice player + clickable photo, lightbox opens full image. Test photo cleaned from demo. User report: a directive like "Install and onboard all users onto DecisionOS using Ramesh's list" was structured into several tasks for the SAME person, which is confusing since each task already gets its own AI Execution Guide (step breakdown). Added a TASK-GRANULARITY rule to the `ai_extract` prompt (Decision Desk voice/text + WhatsApp text-approval, which flows through `process_voice_note`→`ai_extract`) and an ACTION-ITEM-GRANULARITY rule to `ai_meeting_notes` (Meeting Notes): create exactly ONE task per distinct assignee for a single goal; fold sub-steps into the task description (the guide handles them); only create multiple tasks when work goes to DIFFERENT people/roles or is a clearly separate deliverable. Multi-person split preserved. Verified live via ai_extract: single-person directive → 1 task (sub-steps in description); "Priya installs, Ramesh shares list" → 2 tasks (one per person). Prompt-only change, no schema/data impact; existing tasks untouched. Extracted the CompanyDialog body into a new inline `components/CompanyDetails.js` (loads tenant on mount via useEffect; same profile/products/roles/OS-blueprint/workspace-ID sections and testids) and rendered it at the top of `Settings.js` above the Money & Approvals card. `Layout.js`: the sidebar + mobile-drawer tenant name is now a plain `<p data-testid="tenant-name">` label (no longer opens a popup); removed the `CompanyDialog` and unused `Buildings` imports. `CompanyDialog.js` is now orphaned (kept, no imports). Settings stays owner-only. Verified via screenshot: both cards render, company name prefilled. Save reuses verified PATCH /tenant, /tenant/roles, /tenant/os-blueprint.

## Changelog
- 2026-06: **Code-review round 2.** 🔴 **Wildcard import removed** — `server.py` now imports the ~24 needed names from `core` explicitly (was `from core import *`); this also resolved the **3 "possibly undefined variable"** findings (star-import false-positives). Dropped now-unused imports (`bcrypt`, `jwt`, `uuid`, `random`, `HTTPBearer`, `AsyncIOMotorClient`). 🔴 **17 missing hook deps** — audited every `useEffect`: all are dependency-complete or already carry `eslint-disable` with a reason; no real stale-closure bugs. 🟡 **3 index-as-keys** → stable keys (OTP `otp-${i}`, onboarding progress `step-seg-${i}`, Meetings action items content-based). 🟢 **Console statements removed** (Inbox + Login catch blocks) → 0 remain. Verified: backend pytest 9/9, frontend compiles clean. **Deferred (large/low-value/high-churn on live app — do incrementally per module w/ tests):** refactor 115 high-complexity + 42 reduce + 38 long functions; type-hint coverage (8.2%→); split large-import files (Layout/Inbox/Login).
- 2026-06: **Code-review fixes (Critical + cheap Important).** (Critical-Backend) **Circular import fixed**: created `backend/core.py` foundation (config, db, auth deps, permissions, helpers, `normalize_os_blueprint` split into `_bp_*` helpers → lower complexity); both `server.py` (`from core import *`) and `routers/onboarding.py` now import from `core`, so no router imports `server`. Verified: backend starts clean, pytest 9/9, curl smoke (login/me/tasks/blueprint/dashboard all 200). (Critical-Backend) **Hardcoded test secrets** in `tests/test_iteration{17,29,31,32,33}.py` now read `TEST_OWNER_EMAIL`/`TEST_OWNER_PASSWORD` from env with demo fallback. (Critical-Frontend) **Hook deps**: the flagged run-once effects already carry `eslint-disable-next-line react-hooks/exhaustive-deps` with reasons (AuthContext/Login invite/Login step-7); no stale-closure risk. (Critical-Frontend) **localStorage tokens**: already mitigated — auth now lives in HttpOnly cookies (iter 42); remaining localStorage token is the intentional documented Bearer fallback. (Important-Frontend) **Array-index keys**: editable input-row lists (OsEditor workflows/opTasks/rules/products; CompanyDialog products + OS workflows/opTasks/rules) now use stable `_key` ids to prevent stale-input bugs on mid-list delete. **Deferred (need steer, large/risky/low-value):** high-complexity refactors of `register`/`operating_score`/`compute_employee_stats`/`process_voice_note`; type-hint coverage; big component splits (CompanyDialog/Inbox/Layout/ContactProfile); 72 nested ternaries (cosmetic).
- 2026-06: **P2 batch (3 items).** (1) **Refactor:** extracted onboarding endpoints to `/app/backend/routers/onboarding.py` (os-blueprint gen, /onboarding/suggest, PATCH /tenant/os-blueprint; imported into server.py at bottom to avoid circular imports) and split the onboarding Step-4 UI out of `Login.js` into `pages/onboarding/OsEditor.js` + `OsReady.js`. No behavior change. (2) **Enforced task-level approval:** a task with `approval_required` can no longer be self-completed — `update_task` converts a done→ request into `status='review'`/`approval_status='pending'` and notifies the approver; new `POST /api/tasks/{id}/approve` and `/reject` (auth: owner OR `approver_id`, else 403) finalize it (approve→done/100/approved, reject→in_progress/rejected + notify assignee). Frontend `MyWork` TaskCard: 'Submit for approval' button, approval-status badge (Pending/Approved/Changes requested), and an approver Approve/Request-changes action block on review tasks. (3) **Smart assignment:** `pick_least_loaded_member` routes role-assigned tasks (manual `create_task` + AI `_create_decision_tasks`) to the role member with the fewest open tasks instead of a role pool. Verified: iteration_45 — backend 9/9 + frontend 18/18, 100%. Deferred polish (from review): replace reject `window.prompt` with a shadcn dialog; split more of server.py (~4056 lines) into routers; add GET /api/tasks/{id}.
- 2026-06: **Feature — Industry Templates / AI Operating System at onboarding (Phase 1).** New registration step (enhanced Step 4) that AI-generates a ready-to-use Business Operating System from the chosen industry. Backend (`server.py`): `POST /api/onboarding/os-blueprint` (unauth) → Claude generates `{departments, workflows, operational_tasks, approval_rules}` (normalized/capped by `normalize_os_blueprint`); `RegisterInput.os_blueprint` stored on tenant as `workflow_templates`/`operational_task_templates`/`approval_rules`, and **departments become `tenant.roles`** (single RBAC source of truth); register returns `os_summary` counts; `PATCH /api/tenant/os-blueprint` (team_manage) edits the three template lists post-onboarding. Frontend: `Login.js` Step 4 generates + lets the user hand-curate departments/workflows/operational-tasks/approval-rules/products, then `createWorkspace` provisions and shows a **"Your <Industry> Operating System is Ready."** summary screen (counts: Departments/Workflows/Operational Tasks/Approval Rules) before continuing; `CompanyDialog.js` gained an editable "Operating System" section (add/edit/delete workflows, operational tasks, approval rules → Save). Everything is editable during and after onboarding. Existing tenants unaffected (blueprint fields default empty, still editable). Verified: iteration_44 — backend 14/14 + frontend 22/22, 100%. Deferred to Phase 2: Dashboard Widgets, KPI Templates. NOTE: operational tasks are stored as an editable starter *library* on the tenant (not auto-created as live task cards), per the agreed scope.
- 2026-06: **Feature — Operational Tasks in My Work.** Added a new `task_type` dimension (operational/sales/purchase/production/finance/hr/other) plus operational metadata. Backend (`server.py`): `TaskCreateInput` extended with task_type, op_category, support_id, due_date, due_time, expected_output, approval_required, approver_id, progress; `create_task` persists them and computes due_date from date+time; `TaskUpdateInput` now accepts `progress` + validated `status` (todo/in_progress/waiting/review/done/cancelled); `enrich_task/enrich_tasks` add task_type (derived from stored value or assignee_role fallback), attachment_count, support_name/approver_name/created_by_name. Frontend: `Tasks.js` exports an enhanced `NewTaskDialog` (task-type-select → conditional op-category-select, assigned/supporting employee, priority, due date/time, expected output, approval-required→approver, attachment upload, created-by display); `MyWork.js` list view now has 7 tabs (All default/Operational/Sales/Purchase/Production/Finance/Completed) with count badges, a New Task button, and task cards showing op category chip, progress bar + status/progress selects, approval badge, attachment count, overdue badge. Statuses map: Not Started=todo, In Progress, Waiting, Under Review=review, Completed=done, Cancelled, Overdue=derived. Verified: curl (create/patch persist all fields) + iteration_43 frontend E2E 100% (10/10 flows). NOTE: a task only surfaces in a user's My Work if assigned to them or their role.
- 2026-07: **Security — Auth migrated to HttpOnly cookies** (closes XSS token-theft vuln from code review). Backend (`server.py`): `/auth/login`, `/auth/register`, `/auth/otp/verify` now `set_cookie('dos_token', HttpOnly, Secure, SameSite=None, 7d)`; added `POST /auth/logout` to clear it; `get_current_user` reads the cookie first and falls back to `Authorization: Bearer` (backward-compat during rotation). CORS fixed: `allow_credentials=True` is incompatible with `allow_origins='*'`, so it now reflects the request Origin via `allow_origin_regex='.*'` when CORS_ORIGINS is '*'. Frontend: `api.js` axios `withCredentials:true`; `AuthContext.js` hydrates session from `/auth/me` (cookie is source of truth), `persist()` still keeps a localStorage token as fallback, `logout()` calls `/auth/logout`. Verified: curl (Set-Cookie present, cookie-only `/auth/me` 200, no-cookie 401) + iteration_42 frontend E2E 100% (login, reload-with-localStorage-cleared session restore, logout, post-logout reload stays signed out). NOTE: remove the localStorage Bearer fallback once all sessions rotate to fully realize the XSS benefit.
- 2026-07: Code-quality refactor #2 — decomposed the three flagged mega-functions in server.py (pure refactor, behavior preserved). `process_voice_note()` → extracted `_create_decision_tasks`, `_create_reminders_and_memory`, `_create_meetings`, `_create_workflows`. `operating_score()` → extracted `_is_open_task`, `_score_execution`, `_score_sales`, `_score_employees`. `add_task_update()` → extracted `_resolve_task_handoff`. Verified via curl: structuring produced identical execution_summary (tasks/workflow/meeting/reminder/memory); operating-score output byte-identical (overall=18, same categories, 4 employees); task note/escalate/handoff all create correct follow-ups. Also applied safe review fixes: DEMO creds now read from env (server.py), documented intentional empty-deps in AuthContext/Login/Layout (linter false positives), Calendar list key uses stable ref_id.
- 2026-07: Workflow taxonomy rework (payment follow-up misclassified as an order). (1) AI now routes "follow up / chase / collect payment on an invoice" to a FINANCE TASK (not a workflow card) — verified: such a directive produced 0 workflow_events + a 'Follow up on invoice payment' finance task. (2) Split the old single 'sales_dispatch' board into two workflow types: **production** (order_received→confirmed→in_production→ready) and **distribution** (ready_to_dispatch→dispatched→in_transit→delivered); kept **purchase_payment**. Legacy sales_dispatch stages retained for backward-compat rendering, and existing demo cards migrated to the new types. Frontend Workflows page now has 3 tabs (Production/Distribution/Procurement) with matching columns. (3) Added owner-only DELETE /api/workflows/{id} + a per-card trash button (owner only). Verified: backend curl (type counts, delete owner 200/sales 403, AI routing) + iter41 frontend (all 7 scenarios pass; non-owner sees no delete). Added DialogDescription a11y.
- 2026-07: Bug fix — rejecting a decision now removes everything it spawned. Previously reject only set linked tasks to status 'cancelled', so they (and the auto-created workflow/process cards + calendar meetings) still showed up. `POST /api/decisions/{id}/reject` now DELETEs all tasks, workflows, and calendar_events linked by decision_id and dismisses the decision's inbox item; the decision record stays marked 'rejected' for audit. Frontend already refetches tasks/workflows/decisions/inbox after the action. Verified via curl: directive created 2 tasks + 1 workflow → after reject, 0 tasks / 0 workflows / 0 calendar / inbox dismissed / decision status 'rejected'. Test data cleaned.
- 2026-07: Role management + honest Operating Score. (1) ROLES: owners can now add/rename/delete team roles after registration via a new "Team Roles" section in the Company Details dialog (CompanyDialog.js). Backend: POST /api/tenant/roles (slugified key, dup-blocked), PATCH /api/tenant/roles/{key} (rename, key stays stable so member assignments hold), DELETE /api/tenant/roles/{key} (blocked with count message when members are assigned; Owner role protected) — all require 'team_manage'. (2) OPERATING SCORE: previously a brand-new company showed a misleading ~62 due to neutral 0.7 fallbacks + responsiveness=100. Now the endpoint returns overall=null + enough_data=false until there are >=3 actionable tasks or any invoices; OperatingScore.js shows a "Not enough data yet" state (data-testid='operating-not-ready') with progress hints, then switches to the real score. Verified: backend curl (all role scenarios; fresh tenant None/enough_data=false, demo numeric) + iter40 frontend 100%; added DialogDescription for a11y. Demo tenant left with original roles.
- 2026-07: Decision Desk — non-blocking "Structuring in process" indicator. When a voice/text directive is submitted, background structuring (transcribe → LLM extract) now surfaces a sticky yellow banner under the page header (data-testid='structuring-banner' / 'structuring-status') that shows the live stage ('Queued…' → 'Transcribing your voice note…' → 'Structuring in process…', with a count when multiple). It's driven by the existing `processing` flag (notes in queued/transcribing/structuring) + 2.5s poll, does NOT block the UI (page stays scrollable/clickable), and persists until completion, at which point it clears and the Execution Summary opens. Verified iter39: banner appears/persists/non-blocking/clears; Execution Summary follows. 100%.
- 2026-07: Deployment fix — added root-level `@app.get('/health')` (+ `/api/health`) returning {"status":"ok"} so the K8s liveness/readiness probe (GET /health) stops 404-ing. Deployment agent: pass, no other blockers. Root cause: the textarea has no explicit background class, so it kept the browser's default WHITE background while inheriting light text → light-on-white = invisible; the app never set `color-scheme: dark`. Fix (index.css): added `color-scheme: dark` to the `.dark` block (also fixes native selects/scrollbars/autofill) + explicit rule `.dark textarea, .dark input:not([checkbox/radio/range]) { background-color:#16171c; color:#f4f4f5 }`. Verified iter38: directive textarea dark = bg rgb(22,23,28)/text rgb(244,244,245); light mode reverts cleanly. 100%.
- 2026-07: Granular "Voice Box" access. Added a new `voice_capture` permission (OFF by default) so owners can grant a member access to the Decision Desk capture panel. Member Access editor (Team.js MemberDialog) now shows a "Voice Box (Decision Desk capture)" toggle (data-testid='perm-voice_capture'; added aria-pressed). Inbox.js gates the FULL capture panel (voice record button + 'Type a directive' text box) behind `canCapture = owner || hasPerm('voice_capture')` — non-permitted members still see the inbox + an "isn't enabled for your access" notice. Backend: `voice_capture` added to PERMISSION_KEYS; POST /api/voice-notes, /api/voice-notes/text, /api/capture/clarify switched from require_role('owner') to require_perm('voice_capture') (owner still passes). Member directives flow through the existing pending_approval pipeline → surface in owner's Decision Approvals. Verified: backend curl gate (owner 200 / member-without 403 / member-with 200) + iter37 frontend e2e (grant→member sees panel; revoke→clean state) 100%. Hardened the `.dark` scoped overrides in index.css after user reported invisible text/buttons/active states across pages. Key fixes: (1) `bg-brand-yellow` now forces dark text (#0A0A0B) so the ~58 `bg-brand-yellow text-black` chips/buttons (Send Daily Digest, priority/status chips) stay readable instead of flipping to light; (2) `bg-brand-ink` remapped to charcoal #2b2c33 (was pure #000) so primary buttons + active tab/nav states — incl. the Decision Desk mic button — stand out from #16171c cards; (3) added `.dark .text-brand-blue` (#6ea8ff) fixing CEO Brief 'VIEW DETAILS' links + absent count (was 1.67:1, now 7.42:1); (4) softened border-black to #4a4b54, added missing border-black opacity variants (/15,/30,/40,/50), text-black opacity variants, brightened status colors (green/amber/orange), bg-black/20, dark input placeholder color (#6b6b75). Verified iter35 (9-route audit, 8/9 clean) + iter36 (fix confirmed, light mode unaffected). No component rewrites — CSS-only, low regression risk.
- 2026-07: Dark/Light mode toggle + polished Mobile OTP UI (P0). Added a theme toggle to the app header (Layout.js `ThemeToggle`, data-testid='theme-toggle', in both desktop header and mobile top bar) and a floating toggle on /login (data-testid='login-theme-toggle') — Sun/Moon icons via Phosphor, driven by `hooks/useTheme.js` (toggles `.dark` on <html>, persists to localStorage 'decisionos-theme'; pre-mount inline check in index.js avoids FOUC). Extended index.css `.dark` scoped overrides and FIXED a pre-existing unclosed `@layer utilities` block. Reworked the OTP login (Login.js): new `OtpBoxes` 6-box PIN input (auto-advance, backspace nav, paste-fill), masked-phone confirmation row with Change, and a 30s resend countdown (`resendIn`/`startResendTimer`); Verify button disabled until 6 digits. Verified iter34: frontend 100%. Their `/api/Home/Registration?ApiKey=&PhoneNumber=` endpoint SENDS the OTP SMS and RETURNS the same 6-digit code in the JSON body (e.g. `"158932"`). New `_apm_send_and_fetch_otp` helper (httpx async, 10s timeout, regex-extracts the 6-digit code, fails safe with 503) is called inside `_issue_otp`: when `APM_SMS_API_KEY` is set it uses APM's code (real SMS, `dev_mode:false`, no `dev_otp` leaked); otherwise falls back to Twilio, then dev-mode (`secrets` code + `dev_otp`). Env: `APM_SMS_API_KEY`, `APM_OTP_ENDPOINT` (Registration/ForgotPassword) in backend/.env — key never hardcoded. Verified live end-to-end on 7010570560: request → real SMS (branded "Tracole Technologies") → captured code 741027 → verify → JWT login succeeded. Consulted integration_playbook_expert for the secure pattern first (auth integration rule).
- 2026-07: Instant-execution roadmap step #3 — auto-schedule meetings onto the Business Calendar. Detected `meeting_events` now become real `calendar_events` (new collection) with a resolved future date via `_resolve_meeting_date` (today/tomorrow/weekday/"next weekday"/"next week"/due_in_days/default+2; weekday names in `when` take priority over the LLM's due_in_days so "this Friday" lands on Friday). `/api/calendar` aggregates them as `type="meeting"` (UsersThree dark tile); Calendar.js shows a "Meetings" filter (listed first). Execution Summary chip relabeled "meeting(s) scheduled" (was "detected"). Each meeting also creates an undated source='meeting' to-do (visible in My Work, no calendar dup). Inbox auto-invalidates the calendar/workflows/tasks queries when a directive finishes so new items appear without a full nav. Verified iter33: backend 5/5, frontend 100%, no bugs; fixed a post-test date-drift UX nit. NEXT: #4 enforce approval gates, #5 smart multi-person assignment.
- 2026-07: FIX — Execution Summary panel didn't appear for SPOKEN directives. Root cause: the voice/audio upload path in Inbox.js (`POST /voice-notes`) never captured the returned note id, so `submittedNoteId` stayed null and the reveal effect never fired (only the typed `/voice-notes/text` path set it). Fix: capture `data.id` → `setSubmittedNoteId` on audio upload; added a guard so the panel only opens when at least one count > 0 (skips garbled/empty audio) and clears tracking on `failed` status. Voice now behaves identically to the verified typed path. Also cleaned the Sharma demo tenant of heavy accumulated test pollution from 30+ prior iterations: removed ~95 TEST_ tasks, ~93 duplicate tasks, ~40 duplicate decisions, Toyota/dispatch/spindles/Kapoor test directives, garbled-transcript items, stale inbox-feed rows, and 1 stray team member — demo trimmed to 4 core users, ~21-item activity feed, 0 stale pending approvals.
- 2026-07: Instant-execution roadmap step #2 — auto-materialize workflows. `ai_extract` workflow_events schema enriched to {type, title, detail, counterparty, amount} and the prompt now scopes them to CONCRETE order-fulfilment/procurement items (policies go to memory_notes instead). `process_voice_note` inserts a real Workflow board card per event (stage=stages[0], source='voice', decision_id linked, best-effort contact_id match by counterparty), collects `workflow_ids` on the decision, and sets `execution_summary.workflows` to the count of ACTUAL cards created. Execution Summary chip relabeled "workflow(s) generated" (was "flagged"). Verified iter32: backend 4/4, frontend all pass — Toyota+spindles directive → 2 linked cards (Dispatch Toyota Order @order_received; Purchase Order 50 Spindles from Rajesh Traders @requested, ₹80k); policy directive → 0 cards. Also purged 121 stale TEST_ workflow cards polluting the demo board. NEXT: #3 real calendar events for meetings, #4 enforce approval gates, #5 smart multi-person assignment.
- 2026-07: Decision Desk "Execution Summary" panel (#1 of the instant-execution roadmap). After a directive finishes AI structuring, an animated panel reveals REAL counts of what was produced — tasks created, people assigned (de-duped by user/role), approvals to review, workflows flagged, meetings detected, reminders set — with count-up + staggered reveal and a closing line ("ready to execute" / "approve to activate"). Option A labels (accurate: "flagged/detected", not "generated") since workflows/meetings are only detected this phase. Backend: `ai_extract` gained a `meeting_events` field; `process_voice_note` tracks assignee_keys, creates a source='meeting' follow-up task per meeting, and stores `execution_summary` on both the decision and the voice_note (surfaced via GET /api/voice-notes polling). Frontend: new `components/ExecutionSummary.js`, wired in Inbox.js (submittedNoteId → useEffect opens panel on done; Review&approve sets ?focus=approval:<id> to scroll to the card). Verified iter31: backend 4/4, frontend 3/3 (happy path, reminder-only "Done" variant, approve regression). Applied a11y fix (DialogTitle/Description). NEXT roadmap steps: #2 auto-materialize workflows onto the board, #3 create real calendar events, #4 enforce approval gates, #5 smart multi-person assignment.
- 2026-07: Auto-generated invite link/SMS. Adding a member (with a phone) now returns an `invite_token`; owners get a copyable one-tap invite link + WhatsApp share (Team.js InviteLinkModal), plus a per-row "Invite" button that regenerates a link via `POST /api/users/{id}/invite`. Link format `<origin>/login?invite=<token>` (must be /login, since / is auth-guarded and drops query). New public endpoints `GET /api/auth/invite/{token}` (welcome: name, masked phone, company) and `POST /api/auth/invite/{token}/start` (sends OTP, returns phone+dev_otp). Login.js reads `?invite=`, auto-selects Mobile-OTP tab, shows a welcome banner, prefills phone and (dev) auto-fills the code. invite_token/expiry excluded from GET /users. Fixed a StrictMode double-invoke race in the invite useEffect with a `useRef` guard. Verified: testing agent iter30 backend 18/18 pass, owner UI flows pass; main agent confirmed full deep-link → Verify → signed-in flow works. Invite links expire in 7 days.
- 2026-07: Passwordless first-time invite. `POST /api/users` now accepts members WITHOUT a password (`UserCreateInput.password` optional). When omitted, a valid mobile is required and the member is stored with `passwordless: true` and an un-guessable random password_hash (so email/password login always 401s) — they sign in only via mobile OTP. Add Member dialog (Team.js) has a "Password Login | Mobile OTP" toggle: OTP mode hides the password field, requires phone, and shows a helper hint. Verified via curl: create passwordless → otp/request → otp/verify issues JWT; password login returns 401.
- 2026-07: APM Technologies SMS assessed as INCOMPATIBLE with our backend-verified OTP flow — its only 2 endpoints (/Registration, /ForgotPassword) generate & send APM's own OTP with no verify endpoint and no custom-message send, so the backend can never learn/verify the code. Left OTP delivery in secure dev-mode (backend generates+verifies; only SMS send mocked, Twilio-ready). Awaiting user: APM /Verify or generic Send-SMS endpoint, or Twilio keys.

## Changelog
- 2026-06: Added alternate login: Mobile number + OTP for already-registered users. Backend POST /api/auth/otp/request (finds user by phone, generates 6-digit OTP hashed in db.otp_codes, 5-min expiry, 5-attempt max, 30s resend cooldown) and /api/auth/otp/verify (issues same JWT). DEV mode returns dev_otp until Twilio keys added (clean `_send_otp_sms` switch, TWILIO_ACCOUNT_SID/AUTH_TOKEN/FROM_NUMBER). Added `phone` to signup, Add/Edit Member, and Company profile; seeded demo users with mobiles. Frontend Login has Email/Password | Mobile OTP tabs. Verified by testing agent iteration_29 (15/15 backend, OTP + email regression pass).
- 2026-06: Nav order set (web sidebar: Decision Desk, CEO Brief, My Work, People, Company Brain, Capture, Workflows, Meeting Notes; mobile bottom bar top-5: Desk, Brief, Work, People, Brain). Added `data_input` to base perms so all employees get Capture by default. Added a live "This member will see these menus" preview in the Team Access editor that updates as permissions are toggled.
- 2026-06: Enforced strict RBAC. Route guards via `Protected({perm, ownerOnly})` rendering an Access Denied screen; backend `require_perm`/`require_role` added to contacts(people), workflows(workflows), brain(brain), ask(ask), ingest(data_input), operating-score(owner-only). CEO Brief is now role-aware: owner = company-wide brief + fires banner + CEO Journal button; employees = personal "My Brief" (own delayed/todo/in_progress/completed/escalations/handoffs, drill-downs scoped to them, no fires) + an "AI Coach" button that opens their own Work Coach. Also removed company profile/products cards from People>Employees (now edited only via the top-bar company dialog / PATCH /api/tenant). Verified end-to-end by testing agent (iteration_27: 30/30 backend, all frontend RBAC flows pass).
- 2026-06: Company name below the sidebar logo is now a button that opens a Company Details dialog — shows all company fields + Products & Services. Owners/managers (team_manage) can edit company details and add/edit/remove products; others see read-only. Added backend `PATCH /api/tenant` and `refreshTenant()` in AuthContext. Also: removed "Work Coach" from sidebar (still reachable via per-employee Coach buttons in Operating Score); replaced Decision Desk "Upload Data" button with "Operating Score" and removed Operating Score from sidebar.
- 2026-06: Merged "Team" into "People" as a hub with Employees | Customers | Vendors tabs. Extracted `TeamPanel` (Team.js) and `ContactsPanel({types})` (Contacts.js); new People.js container; removed Team nav; `/team` redirects to `/contacts`. Customers tab = customer+dealer, Vendors = vendor.
- 2026-06: Removed "Priorities" menu; added an "AI Priority" toggle button inside My Work. When on, My Work re-orders into a "Priority Line" (sorted by AI priority_score) and each task card shows Impact/Revenue/Risk/Urgency bars + score via `POST /tasks/prioritize`, while all task actions keep working. `/priorities` redirects to `/my-work`; deleted Priorities.js.
- 2026-06: Merged "Ask AI" into "Company Brain" as tabs — Ask (LLM chat via `POST /ask`, default) and Search (keyword record browser via `GET /brain/search`). Extracted `AskPanel` from AskAI.js and `SearchPanel` in Brain.js; removed Ask AI nav; `/ask` redirects to `/brain`. (Technical note: Brain search = deterministic DB lookup returning records; Ask = RAG LLM answer with citations.)
- 2026-06: Merged "Tasks" and "My Work" into a single "My Work" menu with a My Work | Board toggle (default: My Work). My Work = rich execution cards (AI Execution Guide, photo/voice proof, handoff/escalate, Messages). Board = Kanban with owner All/My filter, New Task, reassign, Move-to buttons. Extracted `TaskBoard` from Tasks.js; removed Tasks nav; `/tasks` redirects to `/my-work`.
- 2026-06: Nav cleanup — renamed Inbox→"Decision Desk", Data Input→"Capture"; removed Calendar and CEO Journal from sidebar (Journal reachable via a button inside CEO Brief); added pulsing fires badge on CEO Brief nav.
- 2026-06: Added CEO Brief "Fires to put out today" red banner (escalation count) that drills into the escalations list. Every drill-down item is now clickable and deep-links to its source page: approvals → Inbox decision card (scrolls + red-ring highlight, shows auto-assigned team + Add member + Approve/Reject), escalations → Inbox "Needs Your Attention", purchases/payments → Workflows, complaints → People, absent → Team, tasks → Tasks. Backend: added `fires` counter to `/api/brief` and `fires` key to `/api/brief/details`; Inbox reads `?focus=approval:<id>|attention:<id>` to scroll/highlight.
- 2026-06: CEO Brief blocks are now interactive drill-downs. Each counter card (delayed, completed, awaiting approval, absent, complaints, payment overdue) is clickable and opens a modal listing its items with in-place actions — Approve/Reject decisions, Review purchases in Workflows, Mark complaints resolved. Removed the scroll-down actionable section. Brief counters auto-refresh every 30s. Added backend `GET /api/brief/details?key=&period=`.
- 2026-06: Merged "Daily Brief" into "CEO Brief". Removed the separate Daily Brief nav item + Dashboard.js page. CEO Brief now shows period counter cards (Morning/Evening/Weekly/Monthly) plus the actionable Daily Brief blocks (Pending Approvals w/ approve-reject, Overdue Tasks, Wins Today, Recent Activity). `/dashboard` redirects to `/brief`.


# DecisionOS — PRD & Build Log

## Original Problem Statement
AI operating system for founder-led SMEs that captures spoken decisions, turns them into structured tasks, and gives teams a shared operational brain. Voice-first owner inbox, AI extraction pipeline, Company Brain, Ask AI, flagship workflows (Purchase→Payment, Sales→Dispatch), multi-tenant role-based access (Owner, Sales, Production, Finance), daily brief dashboard, async voice-note processing, email digest (Resend), Sharma-style demo seed.

## Architecture
- **Frontend**: React 19 + Tailwind (Brutalist "Swiss/High-Contrast" theme: Chivo + IBM Plex Mono, red/blue/yellow accents, rounded-none, hard offset shadows). Phosphor icons. React Query. Bearer-token auth in localStorage.
- **Backend**: FastAPI (single `server.py`), all routes under `/api`. JWT Bearer auth, bcrypt. Async voice pipeline via BackgroundTasks.
- **DB**: MongoDB (uuid string ids, ISO datetimes, `_id` excluded). Collections: tenants, users, decisions, tasks, workflows, voice_notes, activity.
- **AI**: Emergent Universal Key — Claude Sonnet 4.6 (extraction + Ask AI), OpenAI whisper-1 (transcription).
- **Email**: Resend (ready-to-plug; RESEND_API_KEY empty → digest returns mocked preview HTML).

## User Personas
- **Owner**: records/types directives, approves decisions & purchases, sends digest, manages team.
- **Sales / Production / Finance**: execute tasks, advance workflows in their lane, review Company Brain, Ask AI.

## Core Requirements (static)
Multi-tenant isolation, role-based access, voice+text capture, AI structuring with approval gating & task unblocking, linked-record Company Brain, natural-language Ask AI, two flagship workflows with stage gates, daily brief.

## Implemented (2026-07-01)
- JWT email/password auth; register creates tenant+owner; demo Sharma workspace auto-seeded (owner/sales/production/finance @sharma.com / demo1234).
- **Industry-aware onboarding**: 3-step registration wizard captures industry, company size, region, currency; AI (`/api/onboarding/suggest`) proposes team roles + products/services (editable). Roles are now **dynamic per tenant** (validated via `tenant_role_keys`); Owner implicit. Company profile shown on Team page; currency-aware amount formatting; workflows relabeled generically (Order Fulfilment / Procurement).
- Owner Inbox: in-browser mic recording (whisper) + text fallback; async job feed; owner-only posting.
- AI extraction: transcript → Decision (pending_approval) + linked blocked Tasks (assignee_role constrained to tenant roles); owner approve unblocks, reject cancels.
- Tasks board, Workflows kanban (next-stage-only, owner-gated purchase approval), Company Brain search, Ask AI, Daily Brief dashboard, mocked Resend digest, Team management.
- Fully responsive mobile app-style shell (top bar + drawer + bottom tab nav).
- **Contacts CRM** (`/contacts`): combined customers & vendors with company/phone/email/address/GST/tags/status/assigned-owner/notes; Owner+Sales manage (finance/production read-only); linked into workflows via contact picker; searchable in Company Brain and queryable via Ask AI.
- **Multilingual voice**: Owner Inbox language selector (Auto / English / Tamil / Tanglish); Whisper transcription (language/prompt) + Claude extraction understand all three and output structured English tasks.
- Tested: backend 58/58 pytest, frontend 100% across 7 iterations. No product bugs (one cosmetic hydration warning on an `<option>`, non-functional).

## Implemented (2026-07-02) — Data Input + Unified Inbox + 360° + Premium Onboarding
- **Data ingestion pipeline** (`/ingest`): PDF/image invoice OCR via Gemini vision (`gemini-2.5-flash`) and CSV/Excel import with AI auto-detect (Claude). Upload → AI extraction → editable **review panel** → File it → creates Contacts, Invoices/Bills, Payments + auto follow-up Tasks. New collections: `invoices`, `payments`, `ingestions`. Endpoints: `POST /api/ingest/document|csv`, `POST /api/ingest/{id}/commit`, `GET /api/ingest`, `/invoices`, `/payments`. Filed invoices searchable in Company Brain. WhatsApp webhook stub (`/api/webhooks/whatsapp`) ready, returns not_configured until keys added.
- **Unified Inbox** = the new PRIMARY home screen (`/`). Merges voice/text capture + uploads + incoming items; every item AI-classified into Customer/Supplier/Invoice/Payment/Complaint/Task/Approval/Reminder. Filter chips w/ open counts, mark done/dismiss, plus owner Pending-Approvals. New `inbox` collection + `add_inbox_item` helper wired into voice processing, ingestion (open→done on commit), complaints. Endpoints: `GET /api/inbox`, `POST /api/inbox/{id}/status`. Old Daily Brief dashboard moved to `/dashboard`.
- **360° Customer/Supplier profile** (`/contacts/:id`, Owner+Finance only): aggregates contact details, sales/purchase bills, payments, **outstanding** (Σ billed − Σ paid), follow-ups, complaints, tasks, decisions; suppliers add pending deliveries + price history. `GET /api/contacts/{id}/profile`.
- **Owner-level Ask AI over money** (Owner+Finance): `/api/ask` context extended with invoices, payments, per-party outstanding, company currency and today's date — answers "who owes the most", "not paid in 30 days", "yesterday's sales". Money data withheld from Sales/Production.
- **Premium 6-step onboarding** ("Digital Executive Office"): Step1 company+account+GST+branches, Step2 business scale, Step3 current software, Step4 connect (Excel live via ingestion; Tally/Zoho coming-soon), Step5 invite employees by mobile (pending invites), Step6 animated "AI learns business". Workspace is created at end of Step3 so Steps 4-5 run authenticated. Tenant now stores gst/branches/business_scale/current_software/invited_employees. Endpoints: `GET/POST /api/invites`.
- Tested: iterations 11-14 — backend 62/62 (+regression), frontend E2E across owner/finance/sales/production. No product bugs.

## Task allocation to team members (2026-07-03)
- Tasks can be assigned to a specific person (assignee_id), not just a role. New Task dialog has a member picker + role fallback; each task card shows the assignee (person chip / role / Unassigned) and a "Reassign to…" dropdown. Backend validates assignee_id against tenant users and auto-derives assignee_role from the member; status-only PATCH preserves the assignee (exclude_unset).
- Voice/text directives resolve named people: `ai_extract` receives team member names and emits `assignee_name`; `match_member_by_name` (exact → first-name/token) maps it to a member so the task is assigned to that exact person, else falls back to role-based auto-assignment. Verified iterations 16 (47/47) & 17 (55/55).

## WhatsApp Cloud API ingestion (2026-07-02)
- Live Meta WhatsApp Business Cloud API webhook wired into the ingestion pipeline. `GET /api/webhooks/whatsapp` (verify handshake) + `POST /api/webhooks/whatsapp` (X-Hub-Signature-256 validated when WA_APP_SECRET set; processing in BackgroundTasks, returns 200 immediately).
- Incoming image/PDF → download media via Graph API (`v21.0`) → `ai_extract_document` → auto-`commit_ingestion_records` → ingestion + inbox item (source=whatsapp) → confirmation reply to sender. Text messages → structured via `process_voice_note`.
- Tenant resolution: sender phone matched against `tenants.invited_employees`, else `WA_TENANT_ID` env. Owner user used as created_by.
- Env (backend/.env): WA_ACCESS_TOKEN, WA_PHONE_NUMBER_ID, WA_VERIFY_TOKEN, WA_APP_SECRET, WA_TENANT_ID, GRAPH_API_VERSION. Empty until user provides → endpoint returns not_configured.
- Verified: GET verify returns challenge; POST text message created a whatsapp-sourced note structured into inbox. Media path reuses already-tested ingestion pipeline (not exercised live pending real creds).
- **Module-level per-employee permissions**: 9 access keys — inbox, data_input, people, finance, workflows, tasks, brain, ask, team_manage. Team "Add member" and per-member "Access" dialogs let the owner pick exactly what each employee can open/use (role-select pre-fills sensible defaults, editable). Endpoints: `POST /api/users` (permissions), `PATCH /api/users/{id}` (role+permissions), gated by `team_manage`.
- Enforcement: owner bypass; users with no `permissions` fall back to ROLE_DEFAULT_PERMS (sales→+data_input+people; finance→+data_input+finance; others→base). Backend `require_perm` guards Finance (`/invoices`,`/payments`,`/contacts/{id}/profile`, Ask-AI money), Data Input (`/ingest/*`), Team management (`/users`,`/invites`). Frontend hides nav items (Layout) and page actions (Ingest/Contacts/Inbox/ContactProfile) via `lib/perms.js` `hasPerm`.

## Bug fix — Owner couldn't find escalations (discoverability) (2026-07-08)
Reported: production escalated a task but the owner had no obvious place to see it. Root cause was discoverability — the backend already delivered escalations (owner notification + follow-up task in My Work) but nothing surfaced them. Fix: (1) escalation follow-up tasks now tagged `source='escalation'` (handoffs stay `source='handoff'`); (2) the Inbox shows a prominent red "Needs your attention — N escalation/handoffs" section (`escalations-section`) listing source=escalation/handoff tasks assigned to the current user, each linking to My Work; (3) My Work task cards show an Escalation (red) / Handoff (blue) badge; (4) Inbox refresh() invalidates the tasks query so new items appear immediately. Verified iteration_26: 8/8 backend + full frontend E2E (production escalate → owner sees red Escalation badge in Inbox section + notification + My Work; handoff to member tagged correctly; non-owner only sees incoming handoffs). Zero bugs.

## Feature — AI Clarification Questions (Phase 2 complete) (2026-07-08)
When an OWNER types an incomplete directive in the Inbox capture box (e.g. "Collect 200000"), DecisionOS asks up to 4 short clarifying questions (who/which invoice/deadline/instructions) BEFORE structuring. `POST /api/capture/clarify {text}` (owner-only; 403 non-owner, 400 empty) → {complete, questions:[{id,question,hint}]} via `ai_clarify_directive` — a pure pre-check that creates nothing. Complete directives skip straight to structuring. Frontend (Inbox): "Structure it" first runs the clarify check; if incomplete, a clarify-panel shows the questions with hint placeholders + Submit-with-details / Skip / Cancel. Answers are appended as "Details provided:" and posted to the existing /voice-notes/text pipeline; clarify failure never blocks capture. Verified iteration_25: 8/8 backend + full frontend, owner-gated, zero bugs. This completes the AI Execution Guide roadmap (clarification → plan → steps + Ask AI → note/handoff/escalate → work coach).

## Feature — Step Note & Handoff/Escalation (AI Execution Guide) (2026-07-08)
Employees can capture a finding on any execution step (or the task) and log it, hand it off, or escalate. `POST /api/tasks/{id}/updates {text, step_id?, action:'note'|'handoff'|'escalate', to_id?, to_role?}` (assignee/owner only): appends to `task.updates` (with resolved step_text, author, recipient). For handoff/escalate it KEEPS the original task and creates a LINKED follow-up task (`source='handoff'`, `parent_task_id`, carries `decision_id`) assigned to the chosen member / role / owner, pushes an in-app notification ([Handoff]/[Escalation]), and logs a decision timeline event if linked. Frontend (MyWork): per-step "Update" button + a task-level "Activity & Handoffs" trail with a Log Note / Hand Off / Escalate form (member+role pickers); trail shows author → recipient with step context. Verified iteration_23: 13/13 backend + full E2E (production hands off to sales, escalates to owner; both receive follow-up task + notification), zero bugs.

## Bug fix — My Work leaked other members' tasks (2026-07-08)
Reported: My Work showed other team members' tasks. Root cause: `GET /api/tasks?mine=true` filtered on `assignee_role == my role`, which also matched tasks assigned to OTHER specific members of the same role (person-assigned tasks derive assignee_role). Fix: the `mine=true` branch now requires `assignee_id == me` OR (`assignee_id == None` AND `assignee_role == my role`) — i.e., my own tasks + unclaimed role-pool tasks only. The non-owner team board (`mine=false`) still shows the full role lane (intended). Verified iteration_22: 10/10 backend + frontend, other-member leakage = 0, both regressions (team-board lane, owner all-tasks) intact.

## Feature — Personal AI Work Coach (Phase 2, part 1) (2026-07-08)
Per-employee performance review. `GET /api/work-coach?user_id=` returns {target, stats(completed/open/overdue/completion_rate/proof_upload_rate/plans_used/plans_completed/photos/voice), cached summary}; `POST /api/work-coach/refresh` computes stats + Claude-generates {headline, strengths[], improvements[], recommendation} cached on the user doc. Own coach for anyone; owner can view any employee (others 403, missing 404) via `_resolve_coach_target`. Frontend: `/coach` page (nav "Work Coach") with stat cards + AI review sections; owner reaches an employee's coach via a "Coach" link on each Operating Score leaderboard row (`/coach?user=<id>`). Non-owner hitting another's coach shows a clean Access-Denied state (no data leak, no infinite spinner). Tested iteration_21: 14/14 backend + frontend flows; the one MEDIUM UX bug (infinite loading on 403) was fixed and re-verified. Phase 2 remaining: AI Clarification Questions on incomplete owner directives.

## Feature — AI Execution Guide (Phase 1) (2026-07-08)
On "My Work", employees turn a task into an AI-guided execution checklist. Endpoints (owner or task assignee/role only): `POST /api/tasks/{id}/execution-plan/generate` (Claude drafts a context-aware, task-type-classified checklist → stored as `task.execution_plan{status,steps[{id,text,done}],progress,task_type}`); `PATCH /api/tasks/{id}/execution-plan` (save/customize steps: add/edit/remove/reorder, accept; recomputes progress; auto-moves task todo→in_progress on first check and →done at 100%); `POST /api/tasks/{id}/steps/ask` (per-step AI suggestion/script + objection→response handling). Frontend: `ExecutionPlan` component in MyWork with Generate button, edit mode (reorder/add/remove + Accept & start / Save), accepted mode (live progress bar, checkboxes, per-step "Ask AI" panel, Customize). Tested iteration_20: 12/12 backend + 100% frontend, zero bugs. Phase 2 (AI clarification questions + Personal AI Work Coach) deferred as agreed.

## Feature — Add team members to a pending decision before approving (2026-07-08)
On the owner's Pending Approvals (Inbox), each decision now lists its tasks with the exact assignee (person chip or role) and an "Add team member / task" control. New endpoint `POST /api/decisions/{id}/tasks` (owner-only, TaskCreateInput) links a task to the decision: status `blocked` while the decision is pending (unblocks on approve), `todo` if already approved, `cancelled` if rejected; pushes into `decision.task_ids`, logs a timeline "Task added for X" event. `enrich_decision(s)` now runs tasks through `enrich_tasks` so assignee names show. Verified: adding a member to the pending "Chennai counter" decision created a blocked task for Amit Verma; approve unblocked all tasks to todo.

## Bug fix — Role-scoped Tasks visibility (2026-07-08)
Reported: logging in as Production showed ALL tasks (Sales + Finance too). Root cause: `GET /api/tasks` returned every tenant task to any user unless `?mine=true`; the Tasks board defaulted to `mine=false`. Fix: non-owners are now always scoped to their lane (`assignee_id == me OR assignee_role == my role`) in both `list_tasks` and `prioritize_tasks`; owner retains full visibility + optional `?mine` view. Because person-assigned tasks derive `assignee_role` from the member, a Production user sees the whole Production team lane (role tasks + any production member's tasks). Frontend: Tasks page shows a "<role> LANE" badge for non-owners (All/My toggle kept for owner only). Verified via curl (owner 263 all-roles; production 45 production-only; sales/finance likewise) + Production UI screenshot showing the Nylon99 production task in-lane.

## Operational Brain Roadmap — Phases 1-4 (2026-07-08)
Implemented all 4 phases of the user's 8-point "operational brain" expansion. All backend AI uses Emergent LLM Key (Claude Sonnet 4.6). Tested: iteration_18 (Phase 1&2, 55/55 backend + 100% FE) and iteration_19 (Phase 3&4, 12/12 backend + 100% FE).

- **Phase 1 — Decision Timeline & CEO Journal**: Every decision carries a git-style `timeline` array (`add_decision_event` helper) with events on capture/approve/reject/task-status. `GET /api/decisions/{id}/timeline`. CEO Journal (`GET /api/journal?q=`) groups decisions + memory notes by day, newest first. Frontend: `/journal` (nav "CEO Journal", brain perm) with date sections, "Today you decided…", and a Timeline dialog per decision.
- **Phase 2 — AI Task Prioritization**: `POST /api/tasks/prioritize?force=&limit=25` scores open tasks on 4 axes (business_impact, revenue, risk, urgency) + blended priority_score + reason via `ai_score_tasks` (chunks of 25, ~30s for a full batch). Scores CACHED on task docs (`ai_scores`, `scored_at`); non-forced calls skip already-scored. Frontend: `/priorities` (nav "Priorities", tasks perm) ranked list with score + 4 axis bars + Re-prioritize button.
- **Phase 3 — AI Relationship Graph & Business Calendar**: `POST /api/contacts/{id}/rescore` (finance) computes relationship_score + risk_score + reason + signals via `ai_score_contact`, cached on contact (`ai_relationship`), surfaced in profile. `GET /api/calendar?days=45` unifies payment-due (finance-only), task deadlines, deliveries, complaints, birthdays into a date-grouped agenda with overdue flags. Contacts now have optional `birthday`. Frontend: ContactProfile "Relationship Intelligence" card + `/calendar` (nav "Calendar") with type-filter chips.
- **Phase 4 — AI Meeting Notes & Operating Score**: `POST /api/meetings` (audio→Whisper) / `/api/meetings/text` → background `process_meeting` → Claude minutes (title/summary/key_points/decisions/action_items) + auto-creates tasks (source=meeting) assigned by name. `GET /api/meetings`, `/api/meetings/{id}`. `GET /api/operating-score` returns deterministic company score (execution/finance/sales/responsiveness, weighted) + per-employee execution leaderboard; finance category & outstanding hidden (null) for non-finance roles. Frontend: `/meetings` (mic + paste transcript + detail dialog) and `/operating-score` (company gauge + category bars + team leaderboard).

## Backlog / Next
- **P0 (needs user keys)**: WhatsApp Document Ingestion — Meta WhatsApp Cloud API webhook to auto-file forwarded invoices/screenshots via existing `ingest_document` pipeline. Awaiting WHATSAPP_TOKEN / phone-number-id / verify token.
- **P1 (needs user keys)**: Real SMS employee invites (Twilio) — wire into `POST /api/invites`. Zoho Books connector (customers/invoices/payments/bills) — needs Zoho Client ID/Secret.
- **P2**: Tally connector (read-only, local agent bridge). Real Resend digest send. Retry/backoff for transient LLM budget errors. Cursor pagination; split server.py (now ~2730 lines) into routers. Auto-mark invoices paid when payments reconcile against them. Backfill 'created' timeline events on pre-timeline decisions.


## Inbox → Task visibility improvements (2026-07-13)
User confusion: a "TASK" card in the Decision Desk (Inbox) wasn't visible in My Work / Board / Workflows. Root cause (not a bug): Inbox task cards point to a Decision; the generated tasks start `blocked` (Pending Approval) and are assigned to the relevant team (e.g. Sales/Finance), so the owner's personal "My Work" list (mine=true) never showed them. Shipped 3 UX fixes:
- (a) `Inbox.js`: task-type inbox cards (ref_type=="decision") now show a **Board** button → navigates to `/my-work?view=board`.
- (b) `Inbox.js`: a **task-count badge** ("N tasks", yellow "· Pending your approval" when decision status==pending_approval) + inline **Approve** button (owner only) reusing `decide(id,'approve')` → `/api/decisions/{id}/approve`. Joins inbox item to decision via new `decMap` from `decisionsQ`.
- (c) `MyWork.js`: owner-only **My Tasks / All Tasks** scope toggle (`work-scope-toggle`). "All Tasks" calls `/tasks?mine=false` (owner sees all 114 tenant tasks vs 25 personal); each non-operational card shows the assignee line when in All-Tasks mode. Also reads `?view=board` param to deep-link straight to the Board.
Verified e2e via screenshots: All Tasks 25→114 with assignee names; Inbox shows "4 TASKS" badges + Board buttons (22 each). Pending/Approve branch relies on existing verified approval plumbing (no pending decisions in demo data to display live).

## Last-updated / activity stamps on tasks & workflows (2026-07-13)
Added "latest update + relative time" to every task and workflow card. Backend: tasks now persist `updated_at` + `last_action` on create (`Created`), status/progress/assignee change (`Status → …`, `Reassigned`, `Progress N%`, `Submitted for approval`), approve (`Approved`), reject (`Changes requested`), attachment (`Attachment added`). New `_task_activity(t)` helper derives these for legacy docs (from updates[]/execution_plan.updated_at/attachments/approved_at/rejected_at/created_at); wired into `enrich_task` + `enrich_tasks`. Workflows already carry `history[]` (stage/note/at) — frontend reads `history[-1]`. Frontend: new `timeAgo()`/`fullTime()` in `lib/format.js`; a ClockCounterClockwise line "<last_action> · <timeAgo>" (full timestamp on hover title) added to MyWork `TaskCard` (`task-updated-<id>`), Tasks.js board cards (`task-updated-<id>`), and Workflows.js cards (`workflow-updated-<id>`). Verified e2e: My Work 25 lines, Board 138 lines, Workflows 2 lines with correct labels.

## Fix: own company wrongly added as vendor from invoice imports (2026-07-13)
Bug: uploading an invoice issued BY the user's own company (e.g. Tracole→TNSTC) added the OWN company as a vendor and misread direction. Root cause: extractor had no notion of the tenant's own identity and picked the bill-to/company name as counterparty. Fix in server.py:
- `_DOC_SYSTEM` & `_CSV_SYSTEM` prompts now include the tenant company name ("{company}") with rules: never create a contact for our own company; determine direction by issuer (own co = seller → sales_invoice/customer; own co = bill-to → purchase_bill/vendor); contact_name is always the counterparty.
- `ai_extract_document` / `ai_map_spreadsheet` take a `company` arg; callers (ingest/document, ingest/csv, whatsapp inbound) pass `_tenant_name(tenant_id)`.
- Added `_tenant_name()`, `_norm_company()` (strips Pvt Ltd/Ltd/Technologies etc.), and `_is_own()` guard in `commit_ingestion_records` — skips any contact matching the own company in both the contacts loop and `resolve_contact`.
Verified e2e in preview (Sharma tenant): synthetic invoice issued BY Sharma TO Acme → extracted as sales_invoice, customer=Acme, Sharma NOT extracted; commit with Sharma force-injected as vendor → rejected, only Acme created. Test data cleaned up.
Note: terminology — vendor = supplier (we pay them); customer = buyer (they pay us).

## Ingest review upgrade + Supplier terminology + Workspace ID (2026-07-14)
1. Ingest review (`Ingest.js`): added a **direction banner** (Sales Invoice / Purchase Bill / Payment with plain-language explanation, `ingest-direction-banner`), converted Type/Direction free-text to **dropdowns** (`SelectField`) so users can flip customer↔supplier and sales↔purchase before filing (contacts=CONTACT_TYPE_OPTS, invoices=INVOICE_TYPE_OPTS, payments=DIRECTION_OPTS), and an **own-company warning** (`ingest-owncompany-warning`) that highlights + explains when a detected party matches the workspace name (frontend `normCo`/`isOwnCompany` mirror backend guard). Verified e2e: sales invoice → Customer dropdown + banner, own company excluded.
2. Terminology "vendor"→"Supplier" (DISPLAY ONLY; internal value stays `vendor`): added `typeLabel()` + `CONTACT_TYPE_LABELS` in `lib/format.js`; applied in Contacts.js (chip + type select), ContactProfile.js (chip), People.js (tab "Suppliers", "Add Supplier", eyebrow), Workflows.js label, Ingest.js (OPT_LABELS + section headings + hints). Backend CONTACT_TYPES unchanged.
3. WA_TENANT_ID fallback: added owner-only **Workspace ID** block in CompanyDialog (`workspace-id-value` + `workspace-id-copy`) showing `tenant.id` with copy button and WA_TENANT_ID guidance. Verified: shows ae79cf8b-... with Copy.
All in preview — redeploy to push to production. Note: routing is Option B (sender phone → employee's workspace) with WA_TENANT_ID as global fallback for unregistered senders.

## WhatsApp QR + activity log + signature fix (2026-07-15)
CRITICAL FIX: `whatsapp_webhook` used to raise 403 on X-Hub-Signature-256 mismatch. Cloudflare/ingress re-encodes the request body, so Meta's HMAC never matched → every inbound webhook 403'd and DROPPED (root cause of "sent WhatsApp but nothing arrived"). Now signature mismatch is logged as `signature_mismatch` and the message is STILL processed (verified: external POST now 200, was 403).
New: `wa_events` collection + `log_wa_event`/`update_wa_event`; lifecycle logging in `process_whatsapp_message` (received→structured/filed/ignored/error). Endpoints: `GET /api/whatsapp/status` (any auth — config booleans, display_number, wa_number, verified_name via Graph) and `GET /api/whatsapp/logs` (owner-only, last 50 events for tenant + unrouted).
Frontend `Ingest.js`: replaced the static banner with `<WhatsAppCard/>` — big scannable QR (`qrcode.react`, wa.me link), number shown small, owner config chips, and live "Recent WhatsApp activity" log (15s auto-refresh, color-coded status chips) with empty-state debugging guidance. Installed `qrcode.react`.
Verified e2e: status returns +91 73589 36555 / Tracole Decision OS; simulated inbound (valid+invalid sig) logged correctly; QR renders; 5 log rows shown.
All in preview — REDEPLOY to production so real Meta messages stop being dropped.

## WhatsApp Smart Capture — Phase 1 (2026-07-15)
Every inbound WhatsApp message now becomes a **Capture Draft** (nothing auto-executes). AI (`ai_capture_triage` for text, `ai_extract_document` for docs) classifies intent (operational_task/invoice/payment/purchase/sales/hr/meeting/decision/approval/workflow/other), extracts info, and suggests department/reviewer role/priority/due/amount. `persist_capture_draft` routes to a department reviewer role (CAPTURE_ROUTING) and escalates to Owner when policy/high-risk or amount >= CAPTURE_THRESHOLD (₹50,000, env `CAPTURE_OWNER_THRESHOLD`).
Backend endpoints: GET /api/captures (role-scoped; owner sees all), GET /api/captures/pending-count, PATCH /api/captures/{id} (edit), POST /api/captures/{id}/{approve|reject|reassign|clarify}. approve→`execute_capture` reuses existing pipeline: docs→commit_ingestion_records+ingestion+inbox; text→voice_note+process_voice_note then applies reviewer overrides and releases the decision's blocked tasks. Non-owner approving a needs_owner draft → 403. clarify sends a WhatsApp reply back to sender.
Frontend: new `/review` page (`Captures.js`) — Review Queue with status tabs (Pending/Clarification/Approved/Rejected), CaptureCard with classification chips, escalation warning, and Approve/Edit/Reassign/Clarify/Reject actions. Sidebar nav 'Review Queue' with live pending-count badge (Layout.js). `process_whatsapp_message` rewritten to create drafts instead of auto-executing.
Tested (iteration_46): backend 15/15 PASS, frontend 5/5 PASS, retest not needed.
Phase 2 backlog: configurable routing-rules UI, WhatsApp voice-note transcription, clarification round-trip capture, shadcn dialogs instead of window.prompt, drive roles from tenant.roles, extract captures/whatsapp into routers.
All in preview — REDEPLOY to push to production.

## Decision approval auto-advances Procurement workflows (2026-07-15)
User report: two Procurement (purchase_payment) cards stayed in REQUESTED even after approving the decision in Decision Desk. Expected behavior: approving the decision should move its linked procurement workflows requested→approved automatically.
Fix (`approve_decision` in server.py): after unblocking tasks, find all `type=purchase_payment`, `stage=requested` workflows with this `decision_id` and advance them to `approved` (pushes history entry "Auto-approved with decision by {name}") + adds a decision timeline event "{n} procurement workflow(s) advanced to Approved".
Verified via curl: seeded decision+linked purchase_payment workflow → POST /decisions/{id}/approve → workflow stage became `approved`. Backend-only change.
NOTE: existing 2 stuck cards on production were approved BEFORE this fix — they must be advanced once manually (ADVANCE button works). Future approvals auto-advance. REDEPLOY to production to activate.

## Role-based Decision Approval permission (2026-07-17)
User request: decision approval (Decision Desk) should be grantable to employees, not owner-only.
Added new granular permission `decisions_approve` ("Approve Decisions"):
- Backend: added to `PERMISSION_KEYS` (core.py). Changed `approve_decision`, `reject_decision`, and `add_decision_task` from `require_role("owner")` → `require_perm("decisions_approve")`. Owner still passes (owner gets all perms via user_perms).
- Frontend: added to PERMISSIONS list (perms.js) so it appears as a toggle in Team member editor (TeamPanel auto-renders). Inbox.js Decision Desk gating changed from `user?.role === "owner"` → `hasPerm(user, "decisions_approve")` for both the "Decision Approvals" section and the inline approve button.
Verified via curl: employee WITH decisions_approve → approve returns 200; employee WITHOUT → approve/reject return 403. Frontend compiles clean.
REDEPLOY to push to production. Owner grants this per-employee in People → Employees → edit member → permissions.

## Decision approver can assign tasks to team members (2026-07-17)
Follow-up to decisions_approve: the "Assign to a team member" dropdown in the Decision Desk approval card was empty for non-owner approvers because the members query (`usersQ`) was gated to `user?.role === "owner"`. Backend `GET /users` already allows any authenticated user, and `add_decision_task` already uses `require_perm("decisions_approve")`.
Fix (Inbox.js): `usersQ.enabled` now = `hasPerm(user,"decisions_approve") || hasPerm(user,"team_manage")` so decision approvers get the populated member/role dropdowns and can assign tasks while approving.
Verified via curl: approver employee GET /users → 200 (5 members); POST /decisions/{id}/tasks with assignee_id → 200. REDEPLOY to production.

## Leave & Absence Management — Phase 1 (2026-07-17)
Full leave system. Backend (server.py): `leaves` collection + endpoints POST /api/leaves, POST /api/leaves/absence (emergency today), GET /api/leaves?scope=mine|approvals|all, GET /api/leaves/on-leave, GET /api/leaves/{id}, POST /api/leaves/{id}/approve|reject|request-info, PATCH /api/tenant/leave-approvers. Approver resolution priority: reporting_manager_id → tenant.leave_approvers[role] → owner (_resolve_leave_approver). New permission `leave_approve`. Per-employee `reporting_manager_id` added to UserCreate/Update. Notifications fire to approver on submit and to employee on decision (entity_type 'leave'). Approved leaves appear in /api/calendar (type 'leave') and dashboard on_leave_today.
Frontend: Leave.js (Request/Absence dialogs, My Leave/Approvals/Settings tabs, ApproverConfig owner mapping). Team.js MemberDialog got a Reporting Manager dropdown (member-manager-select) + 'Approve Leave' permission toggle.
DESIGN CHANGE (per user): Leave is NOT a sidebar item — it lives inside My Work as a 'Leave' view tab (work-view-leave); /leave redirects to /my-work?view=leave. CEO Brief 'on leave' block removed (employees-absent already covers staffing).
Tested: iteration_50 (22/22 backend, full UI) + iteration_51 (My Work Leave tab + reopen, 100%). Bug fixed: PATCH /users clearing reporting_manager_id (frontend now sends "" not null).
Phase 2 backlog (deferred): AI Impact Analysis on approval (reassign/extend/keep suggestions), SLA reminders to approver, owner daily digest of who's on leave + low-staffing departments, leave attachments (medical certs).

## Task Reopen fix (2026-07-17)
Bug: employees accidentally clicked a task's 'Complete' button and could not bring the task back (all controls were gated on !isTerminal). Fix (MyWork.js TaskCard): Complete now shows a window.confirm; completed/terminal tasks show a 'Reopen' button (reopen-<id>) that PATCHes status→in_progress, progress→0. Verified via curl + iteration_51.
REDEPLOY to production to activate all the above.

## Proof-of-work gallery in CEO Brief completed view (2026-07-17)
User (option b): surface task photo/voice proof for review at a glance. Backend brief_details key 'completed' now joins each task_done activity to its task and returns a `proof` array [{kind,url}] + assignee name as subtitle (kind 'task' so item deep-links to My Work). Frontend CEOBrief.js DetailDialog renders a 'Proof of work · N' gallery per completed item — photo thumbnails (click opens full image in new tab) + inline audio players (clicks stopPropagation so they don't trigger navigation). Reminder of existing behavior: attachments already show on the task card in My Work (My Tasks + All Tasks), attachment-count badge, task trail, and Operating Score proof-rate.
Verified via curl (completed drilldown returns proof url) + compile. REDEPLOY to production.

## Swipe gestures on Decision Desk feed (2026-07-17)
User: replace the on-row View + Dismiss buttons in the 'Tasks & Activity' feed with swipe for a more mobile-friendly UI. Implemented SwipeRow in Inbox.js: swipe RIGHT reveals blue zone → primary view action (openItem for ingestion/complaint, /my-work?view=board for decision-with-tasks, else mark done); swipe LEFT reveals red zone → dismiss. THRESH 70px, MAX 110px, touchAction pan-y so vertical scroll still works; engages only on dominant-horizontal drag. On mobile the button cluster is hidden (hidden sm:flex) + a swipe hint shown (inbox-swipe-hint); on desktop buttons stay visible & functional. Added a threshold commit cue ('Release · View/Dismiss' + brighter zone) when |dx|>=THRESH.
Tested iteration_52: 9/9 frontend PASS (swipe-left dismiss, swipe-right view/board/done, vertical scroll intact, desktop buttons unaffected). REDEPLOY to production.
Deferred nice-to-have: optimistic-hide row on dismiss (currently snaps back briefly before refetch removes it).

## Co-owner: assign Owner role to a teammate (2026-07-17)
User request: owner can promote a partner/employee to Owner. Confirmed: CO-OWNERS (original stays owner), only existing Owners can grant, confirmation required, demotion allowed with last-owner safeguard.
Backend (server.py): create_user + update_user now allow role 'owner' ONLY when acting user.role=='owner' (else 403 'Only an owner can grant the Owner role'); owner role forces permissions=list(PERMISSION_KEYS) (full access); demoting an owner checks owner count and returns 400 'Cannot demote the last owner…' if <=1; imported PERMISSION_KEYS from core.
Frontend (Team.js): roleOptions includes {key:'owner'} only when acting user is owner; member 'Access' edit button shown for owner rows only to acting owners; MemberDialog setRole(owner)=>full perms, hides permission grid + shows owner-access-note for owner role; save() shows window.confirm on promote ('co-owner with FULL control') and demote ('at least one owner must remain').
Verified: curl (promote/co-owner/demote/last-owner 400/non-owner 403) + iteration_53 frontend 17/17 PASS. REDEPLOY to production.

## Fix: WhatsApp payment/invoice captures only visible to owner (2026-07-17)
Bug (user): a bank payment screenshot sent via WhatsApp showed only in the OWNER's review queue, not the finance/accounts role's.
Root cause: persist_capture_draft hardcoded reviewer_role="finance" for money items, but list_captures shows a draft to a non-owner ONLY if their role key == reviewer_role. Tenants whose finance person has a differently-keyed role (e.g. "accounts") → nobody but the owner (who sees all) saw it.
Fix (server.py):
1. Added _finance_role_key(tenant_id, troles): resolves money-item reviewer to the tenant's real finance/accounts role by key/label hints (financ|account|accts|treasur|billing|audit); falls back to owner if none. Money items now route to reviewer = _finance_role_key(...) or "owner".
2. Added reviewer_perm field (="finance" for money items). list_captures + captures/pending-count now show a draft to non-owners where reviewer_role==user.role OR reviewer_perm in user's permissions — so anyone with the FINANCE permission sees finance captures regardless of role-key naming.
Verified: curl (owner + sales-with-finance-perm see it; production-without-perm doesn't) and _finance_role_key unit checks (accounts->accounts, finance->finance, none->None). Backend-only.
NOTE: the user's already-sent capture (created before this fix) lacks reviewer_perm — owner can Reassign it to the finance/accounts person. Future captures auto-route correctly. REDEPLOY to production.
