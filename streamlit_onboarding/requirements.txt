# Standalone onboarding harness — installs into its OWN venv, not the backend's.
# Streamlit needs uvicorn>=0.30 / starlette>=0.40, which conflict with backend/requirements.txt
# (uvicorn==0.25.0, starlette==0.37.2). Keeping them separate avoids upgrading the backend's pins.

streamlit
httpx>=0.26,<1
python-dotenv>=1,<2

# emergentintegrations is a private package hosted on Emergent's own index.
# Install with:
#   pip install -r requirements.txt --extra-index-url https://d33sy5i8bnduwe.cloudfront.net/simple/
emergentintegrations
